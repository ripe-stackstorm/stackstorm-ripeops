---
  version: '2.0'

  ripeops.update_pack:

    input:
      - trigger

    type: direct

    tasks:

      update_pack:
        action: "git.update_pack"
        input:
          packs:
          - '{{ trigger.repository_url }}'
        on-success: "post_update_slack"

      post_update_slack:
        action: "slack.chat.postMessage"
        input:
          text: "foo"
          channel: "ripe-hackathon-atlas"
        on-success: "post_update_slack_2"
      
      post_update_slack2:
        action: "slack.chat.postMessage"
        input:
          text: "foo"
          channel: "ripe-hackathon-go"
